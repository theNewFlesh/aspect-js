{% for item in component.content %}
    {% if item.member_type == 'method' %}
        <div class="definition">
            <div class="definition-header">
                <strong class="method">{{ item.name }}</strong>
                <span>(    </span>
                {% for param in item.parameters %}
                    <span class="param-name">{{ param.name }}:    </span>
                    <i class="param-value">{{ param.type }}</i>
                    <span>):    </span>
                    {% if item.returns != None %}
                        <i class="return-value">{% item.returns %}</i>
                    {% endif %}
                {% endfor %}
            </div>
            <div class="indent">{% item.description %}</div>
            <br>
            {% if item.parameters.length > 0 %}
                <strong>Parameters</strong>
                <br>
                <ul>
                    {% for param in item.parameters %}
                    <li>
                        <strong>{{ param.name }}:    </strong><i class="return-value">{{ param.type }}</i>
                        <br>
                        <span>{{ param.description }}</span>
                    </li>
                    {% endfor %}
                </ul>
            {% endif %}
            {% if item.returns != None %}
                <strong>Returns    </strong><i class="return-value">{{ item.returns }}</i>
                <p>
                    <div class="indent">{{ item.returns.description }}</div>
                </p>
            {% endif %}
        </div>
    {% endif %}
{% endfor %}
